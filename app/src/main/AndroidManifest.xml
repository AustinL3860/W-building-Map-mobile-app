<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ggc.espinosa.wbuildingapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FlooroneActivity"
            android:label="West Hall" />
        <activity android:name=".FloortwoActivity"
            android:label="West Hall" />
        <activity android:name=".FloorthreeActivity"
            android:label="West Hall" />
        <activity android:name=".CampusActivity"
            android:label="West Hall" />
        <activity android:name=".AboutActivity"
            android:label="About" />

    </application>

</manifest>